# iOS Project

Run `npx tauri ios init` on macOS with Xcode 15+ to generate the iOS project.
This directory is a placeholder for the auto-generated iOS Xcode project.

Requirements:
- macOS
- Xcode 15+
- `rustup target add aarch64-apple-ios aarch64-apple-ios-sim x86_64-apple-ios`
